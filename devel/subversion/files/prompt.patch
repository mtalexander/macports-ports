--- subversion/libsvn_subr/prompt.c	2014-04-24 00:01:12.000000000 -0400
+++ subversion/libsvn_subr/prompt.c	2015-03-05 00:51:39.000000000 -0500
@@ -719,7 +719,7 @@
      cert_info->fingerprint);
   svn_stringbuf_appendstr(buf, msg);
 
-  if (may_save)
+  if (may_save || TRUE)
     {
       svn_stringbuf_appendcstr
         (buf, _("(R)eject, accept (t)emporarily or accept (p)ermanently? "));
@@ -736,7 +736,7 @@
       (*cred_p)->may_save = FALSE;
       (*cred_p)->accepted_failures = failures;
     }
-  else if (may_save && (choice[0] == 'p' || choice[0] == 'P'))
+  else if ((may_save || TRUE) && (choice[0] == 'p' || choice[0] == 'P'))
     {
       *cred_p = apr_pcalloc(pool, sizeof(**cred_p));
       (*cred_p)->may_save = TRUE;
